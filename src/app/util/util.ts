import { Car } from "src/app/models/car";
export default class Utils {
  static getCarTitle(obj: Car) {
    return "<app-card-title/>";
  }
  static doSomethingElse(val: string) {
    return val;
  }
}
